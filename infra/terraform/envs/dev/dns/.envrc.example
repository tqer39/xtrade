# CloudFlare 認証設定

# 方法 1: cf-vault を使用（推奨）
# cf-vault add xtrade を実行してプロファイルを追加
# cf-vault exec xtrade -- terraform plan で実行

# 方法 2: 環境変数を直接設定
# export CLOUDFLARE_API_TOKEN="your-api-token"
# export CLOUDFLARE_ACCOUNT_ID="your-account-id"

# AWS 認証（S3 バックエンド用）
# aws-vault add portfolio で設定済みの場合は以下は不要
# export AWS_PROFILE="portfolio"
